import React from 'react';
import '../app.css'; 

const Contact = () => {
    return <div>Contact will have a form with fields for name, an email address and a message.</div>;
  };

export default Contact;
